# Optional launch overlay used by the Prime bridge to route DSH ACP sessions.
- id: acp
  name: '@deepseek-ai/dsh-acp'
  inject: [acpAppStartup]
  config:
    provider: !!js process.env.PRIME_DSH_PROVIDER ?? 'deepseek-official'
    model: !!js process.env.PRIME_DSH_MODEL ?? 'deepseek-v4-flash'
